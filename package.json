{
  "name": "ai-morning",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:news",
    "news:index": "node scripts/news-quality.mjs --write",
    "news:check": "node scripts/news-quality.mjs --check",
    "test:news": "node --test tests/news-*.test.mjs",
    "build": "npm run test:news && npm run news:check && node scripts/prepare-assets.mjs && next build && node scripts/finalize-export.mjs && npm run test:export",
    "dev": "node scripts/prepare-assets.mjs && next dev --hostname 0.0.0.0",
    "test:export": "node --test tests/export.test.mjs",
    "preview": "node scripts/preview.mjs",
    "test:browser": "playwright test",
    "format:check": "prettier --check app components lib next.config.mjs playwright.config.mjs scripts/prepare-assets.mjs scripts/finalize-export.mjs scripts/preview.mjs tests/export.test.mjs tests/browser"
  },
  "engines": {
    "node": ">=20.9"
  },
  "dependencies": {
    "lucide-react": "^1.41.0",
    "motion": "^13.2.0",
    "next": "^16.3.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
